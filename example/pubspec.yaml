name: example
description: Theseus Navigator Demo app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.16.0
  cupertino_icons: ^1.0.2
  google_fonts: ^5.1.0
  theseus_navigator:
    path: ../
  url_launcher: ^6.1.6
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
