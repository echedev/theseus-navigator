name: example
description: Theseus Navigator Demo app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  theseus_navigator:
    path: ../
  url_launcher: ^6.1.5
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
