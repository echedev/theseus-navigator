name: theseus_navigator
description: Theseus Navigator
version: 0.0.1
homepage:

environment:
  sdk: ">=2.13.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test_coverage: ^0.4.2
  utils:
    path: ./utils

flutter:
